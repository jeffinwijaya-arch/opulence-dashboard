{
  "name": "opulence-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=opulence-dashboard",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:py": "python3 -m pytest",
    "test:all": "python3 -m pytest && vitest run"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "jsdom": "^26.0.0",
    "@vitest/coverage-v8": "^3.2.4"
  }
}
